# 为指标生成 department、code、reference、id等字段
# id 使用 uuid
def metric_info_generate(question, formula):
    """
    元信息生成
    """

    return {
        "id": None,
        "department": None,
        "code": None,
        "reference": None
    }
